Index: beast-0.7.1/launchers/beaststart.c
===================================================================
--- beast-0.7.1.orig/launchers/beaststart.c	2008-07-06 15:52:19.000000000 +0200
+++ beast-0.7.1/launchers/beaststart.c	2008-07-06 15:53:20.000000000 +0200
@@ -28,7 +28,7 @@
   const char *bindir = BINDIR;
   const char *name = "beast";
   const char *version = BIN_VERSION;
-  int l = strlen (bindir) + 1 + strlen (name) + 1 + strlen (version);
+  int l = 1 + strlen (bindir) + 1 + strlen (name) + 1 + strlen (version);
   char *string = malloc (l);
   if (!string)
     {
Index: beast-0.7.1/launchers/bseshstart.c
===================================================================
--- beast-0.7.1.orig/launchers/bseshstart.c	2008-07-06 15:52:19.000000000 +0200
+++ beast-0.7.1/launchers/bseshstart.c	2008-07-06 15:53:41.000000000 +0200
@@ -28,7 +28,7 @@
   const char *bindir = BINDIR;
   const char *name = "bsescm";
   const char *version = BIN_VERSION;
-  int l = strlen (bindir) + 1 + strlen (name) + 1 + strlen (version);
+  int l = 1 + strlen (bindir) + 1 + strlen (name) + 1 + strlen (version);
   char *string = malloc (l);
   if (!string)
     {
